from ClientClass import Player
from Num_guesser import NumberGuesser

p = Player("127.0.0.1", 8081)

num = int(input("Enter a number:"))

n = NumberGuesser(num)

guessed_number = n.

guess = p.talk(guessed_number)
